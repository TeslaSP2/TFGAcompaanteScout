<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teslasp2.ftc.acompaante_scout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu"></activity>
        <activity android:name=".actividadesDeProgresoPersonal.AddProgress" />
        <activity android:name=".actividadesDeProgresoPersonal.ShowProgress" />
        <activity android:name=".actividadesDeUsuario.ShowUserSelected" />
        <activity android:name=".actividadesDeUsuario.ShowUsers" />
        <activity android:name=".actividadesDeUsuario.AddUser" />
        <activity android:name=".TemporaryMenu" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>